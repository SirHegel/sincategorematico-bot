const $=s=>document.querySelector(s),login=$("#login"),dashboard=$("#dashboard"),error=$("#login-error");
const escapeText=value=>String(value??"");
async function request(path,options={}){const response=await fetch(path,{headers:{"Content-Type":"application/json"},...options});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.error||"No fue posible completar la operación");return data}
function render(data){login.hidden=true;dashboard.hidden=false;$("#bot-state").textContent=data.initialized?"Conectado":"Pendiente";$("#owner-state").textContent=data.owner?"Propietario verificado":"Sin propietario";$("#publish-state").textContent=data.paused?"En pausa":"En marcha";$("#max-posts").textContent=`${data.max_posts} piezas`;$("#timezone").textContent=data.timezone;$("#pause").disabled=data.paused;$("#resume").disabled=!data.paused;const list=$("#activity-list");list.replaceChildren();if(!data.activity.length){const li=document.createElement("li");li.textContent="Aún no hay actividad registrada";list.append(li)}data.activity.forEach(item=>{const li=document.createElement("li"),dot=document.createElement("i"),text=document.createElement("span"),time=document.createElement("time");text.textContent=escapeText(item.message);time.textContent=new Date(item.created_at*1000).toLocaleString("es-CO",{dateStyle:"short",timeStyle:"short"});li.append(dot,text,time);list.append(li)})}
async function load(){try{render(await request("/api/status"))}catch{login.hidden=false;dashboard.hidden=true}}
$("#login-form").addEventListener("submit",async e=>{e.preventDefault();error.textContent="";try{await request("/api/login",{method:"POST",body:JSON.stringify({token:$("#token").value})});$("#token").value="";await load()}catch(err){error.textContent=err.message}});
async function control(action){await request("/api/control",{method:"POST",body:JSON.stringify({action})});await load()}
$("#pause").addEventListener("click",()=>control("pause"));$("#resume").addEventListener("click",()=>control("resume"));$("#refresh").addEventListener("click",load);load();setInterval(()=>{if(!dashboard.hidden)load()},15000);
